# 部署说明（静态网页）

> 结论：**这是一个纯静态站点**，`npm run build` 出的 `dist/` 直接丢给任何静态托管即可，
> 不需要 Node 服务端、不需要数据库、不需要特殊响应头。

---

## 一、为什么能纯静态

| 通常需要服务端的东西 | 本项目 |
|---|---|
| API / 数据库 | 无。全部逻辑在浏览器里跑 |
| 用户上传落盘 | 无。文件只在内存与浏览器存储里 |
| SPA 路由 rewrite | 无。四个页面是 tab 切换，**没有 URL 路由**，不需要 `try_files` 兜底 |
| `SharedArrayBuffer` 的跨源隔离 | **不需要**。全项目零 `SharedArrayBuffer`，所以**不必配 COOP/COEP 头** |
| CDN / 外部接口 | **一个都没有**。所有资源都是本地文件 |

这三条（无路由、无跨源隔离、无外部依赖）是部署能这么省事的原因，改动时**别不小心破坏它们**：
一旦哪天引入了 `SharedArrayBuffer`（例如换用多线程 wasm），所有托管方都要额外配两个响应头，部署复杂度会立刻上一个台阶。

---

## 二、构建

```bash
npm ci          # 按 lockfile 装依赖
npm run build    # 内含 tsc（主配置 + worker 配置）与 vite build
```

产物在 `dist/`，约 **5.4 MB**（gzip 后约 2 MB）：

| 文件 | 大小 | 何时加载 |
|---|---|---|
| `assets/index-*.js` | 1047 KB（gzip 396 KB） | 首屏 |
| `assets/index-*.css` | 57 KB（gzip 11 KB） | 首屏 |
| `hajimi_audio.wasm` | 1.14 MB | 首次解码 / 变调时 |
| `group1-shard1of1.bin` + `model.json` | 0.9 MB | **仅当用到 AI 音高检测**时懒加载 |
| `assets/register_all_kernels-*.js` | 1.8 MB | 同上，AI 那条链才拉 |

后两行都是懒加载：**用户不碰 AI 功能就不会下载那 2.7 MB**。首屏实际负担只有约 1.1 MB JS + wasm。

---

## 三、两种部署方式

### ① 部署在域名根路径（最常见）

`vite.config.ts` 不用改，直接用 `dist/`。

```bash
npm run build
# 把 dist/ 里的全部内容传到站点根
```

### ② 部署在子目录（如 `https://host/meme-studio/`）

**只需要改一处**——构建时的 `base`：

```bash
npm run build -- --base=/meme-studio/
```

或者永久写进 `vite.config.ts`：

```ts
export default defineConfig({
  base: '/meme-studio/',
  // ...
});
```

⚠️ **改 `base` 就够，代码一个字都不用动。** 这是刻意的设计：
`hajimi_audio.wasm` 与 `model.json` 这两个 `public/` 下的资源**没有模块相对路径可用**
（它们不在模块图里），所以统一走 `import.meta.env.BASE_URL` 拼路径
（`src/engine/rush/loader.ts`、`src/engine/pitch-worker.ts`）。
**不要把它们改回写死的 `/hajimi_audio.wasm`** —— 那样在子目录部署下会 404，
表现为「页面能开、但完全没声音」（wasm = 解码与变调内核）或「AI 音高一直卡在首次加载」。

---

## 四、上线前自查清单

- [ ] `npm run build` 通过，且**没有旧的 `dist/` 残留**（构建会清空输出目录，但被中断过的话会留半成品）
- [ ] `dist/` 里**没有 `_ab/` 目录**（那是探针试听用的 WAV，早先放在 `public/_ab` 会被原样拷进产物）
- [ ] 根路径部署：直接访问首页，琴键能点响
- [ ] 子目录部署：DevTools 的 Network 里确认 `hajimi_audio.wasm` 与 `model.json` 的 URL **带子目录前缀**且是 200（不是 404）
- [ ] `dist/` 里是**构建产物**，不是源码；`.gitignore` 已忽略 `dist/`，别手工把它提交进仓库

---

## 五、别用 `file://` 直接打开

双击 `dist/index.html` 是**不行**的，两类东西会被浏览器拦掉：

1. `module` worker（`new Worker(..., { type: 'module' })`）——`file://` 下属于不透明源；
2. `fetch('.../hajimi_audio.wasm')`——同样受 CORS 限制。

**解码、音高检测、AI 都会失效**，但页面本身能显示，很容易被误判成「网站坏了」。
本地验证请起一个 http 服务，不要双击文件：

```bash
npx vite preview            # 预览 dist/
```

---

## 六、托管方注意事项

- **`.wasm` 的 MIME 建议配成 `application/wasm`**。本项目用
  `WebAssembly.instantiate(bytes)`（非 streaming 版），所以**不配也能跑**；
  但配了更规范，未来若改用 streaming 版就是必需的。主流托管方默认已正确。
- **缓存**：`assets/*` 文件名带内容哈希，可以放心设长缓存（`max-age=31536000, immutable`）；
  `index.html` 与 `hajimi_audio.wasm`、`model.json` **不带哈希，不要长缓存**，
  否则更新后用户拿到的还是旧内核。
- **HTTPS**：麦克风（录制）相关的浏览器 API 要求安全上下文；音频解码与播放本身
  在 http 下也能跑，但建议直接上 HTTPS。
